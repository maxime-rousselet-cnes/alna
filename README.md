# ALNA
Anelastic Love Number Algorithm

We formulate the rheological model using a symbolic implementation to allow for the
semi-analytical computation of partial derivatives in the variational equations of the y_i system.

please install base_models using pip before installing requirements by runnning setup.bash.


TODO: make data_path.txt a larger config: account, n_parameter_values, n_periods.
TODO: Make the corresponding command line argument default values fall back on them.